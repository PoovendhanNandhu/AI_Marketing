[build]
  # Build command
  command = "npm run build"
  
  # Publish directory (.next is used by the @netlify/plugin-nextjs)
  publish = ".next"
  
  # Base directory is root (where package.json is located)
  base = "."

[build.environment]
  # Node.js version
  NODE_VERSION = "18"

# Netlify Next.js plugin handles routing automatically
[[plugins]]
  package = "@netlify/plugin-nextjs"

# Headers for security
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"
    X-Content-Type-Options = "nosniff" 